{
  "name": "wearwave",
  "version": "0.2.0",
  "private": true,
  "description": "WEARWAVE outfit discovery app",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check app.js && node --check server.js && node --check storage.js && node --check vision.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.6.2",
    "file-type": "^22.0.1",
    "helmet": "^8.3.0",
    "multer": "^2.0.2",
    "pg": "^8.23.0"
  }
}
